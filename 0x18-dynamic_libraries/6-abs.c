#include "main.h"

/**
*_abs -> Write a function that computes the absolute value of an integer
*@n: parameter
*Return: n
*/
int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	if (n < 0)
	{
	return (-n);
	}
	return (0);
}
