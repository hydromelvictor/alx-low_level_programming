#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
*str_concat - concatenates two strings
*@s1: parameter
*@s2: parameter
*Retrun: charater
*/
char *str_concat(char *s1, char *s2)
{
	int i = 0, j = 0, k;
	char *str;
	if(*s1 == NULL && *s2 == NULL)
	{
		return (NULL);
	}
	return (1);

}

