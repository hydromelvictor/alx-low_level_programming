#include "main.h"

/**
*print_last_digit -> Write a function that prints the last digit of a number
*@n: parameter
*Return: n%10
*/
int print_last_digit(int n)
{
	_putchar(n % 10);
	return (n % 10);
}
