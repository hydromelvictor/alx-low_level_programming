#include "main.h"

/**
*_abs -> Write a function that computes the absolute value of an integer
*Return: n
*/
int _abs(int n)
{
	if (int print_sign(int n) == 1)
		return (n);
	if (int print_sign(int n) == -1)
		return (-n);
	return (0);
}
