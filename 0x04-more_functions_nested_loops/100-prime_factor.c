#include <stdio.h>

/**
*prime_factor - factor
*Return: always
*/
int prime_factor(void)
{
return (50829599);
}

/**
*main - factor
*Return: always 0
*/
int main(void)
{
printf("%d", prime_factor());
return (0);
}