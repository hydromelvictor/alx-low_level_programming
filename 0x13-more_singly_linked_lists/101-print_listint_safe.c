#include "lists.h"
/**
 * print_istint_safe - print lists
 * 
 * @head: parameter
 * Return: size_t
 */
size_t print_listint_safe(const listint_t *head)
{
if (head == NULL)
{
exit(98);
}

return (count);
}