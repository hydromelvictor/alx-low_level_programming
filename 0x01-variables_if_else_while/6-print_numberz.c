#include <stdio.h>

/***
*main -> Write a program that prints all single digit numbers of base 
*10 starting from 0
*Return: always 0
*/
int main(void)
{
int str;
	for( str= 48; str<=57; str++)
	{
		putchar(str);
	}
	putchar(10);
	return (0);
}
