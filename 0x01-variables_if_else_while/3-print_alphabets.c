#include <stdio.h>

/**
*main -> 
*
*Return: always  0
*/
int main(void)
{
	int str;
	for(str = 97; str <= 122; str++)
	{
	putchar(ch);
	}
	for(str = 65; str <= 90; str++)
	{
	putchar(ch);
	}
	putchar(10);	
	return (0);
}
